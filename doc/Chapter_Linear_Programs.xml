<?xml version="1.0" encoding="UTF-8"?>

<!-- This is an automatically generated file. -->
<Chapter Label="Chapter_Linear_Programs">
<Heading>Linear Programs</Heading>

<Section Label="Chapter_Linear_Programs_Section_Creating_a_linear_program">
<Heading>Creating a linear program</Heading>

<ManSection>
  <Oper Arg="poly, str, obj" Name="Cdd_LinearProgram" Label="for IsCddPolyhedra, IsString, IsList"/>
 <Returns>a <Math>\texttt{CddLinearProgram}</Math> Object
</Returns>
 <Description>
The function takes three variables. The first is a polyhedra <Math>\texttt{poly}</Math>, the second <Math>\texttt{str}</Math> should be max or min and the third <Math>\texttt{obj}</Math> is the objective.
 </Description>
</ManSection>


<ManSection>
  <Oper Arg="lp" Name="Cdd_SolveLinearProgram" Label="for IsCddLinearProgram"/>
 <Returns>a list if the program is optimal, otherwise returns the value 0
</Returns>
 <Description>
The function takes a linear program. If the program is optimal, the function returns a list of two
entries: the solution vector and the optimal value of the objective, otherwise it returns the
value 0.
 </Description>
</ManSection>


<Math>\newline</Math>
To illustrate the using of these functions, let us solve the linear program given by:
<Math>\textbf{Maximize}\;\;\; P(x,y)= 1-2x+5y</Math>, with <Math>\newline</Math>
<Math>100\leq x \leq 200 \newline</Math>
<Math>80\leq y\leq 170 \newline</Math>
<Math>y \geq -x+200\newline\newline</Math>
We bring the inequalities to the form <Math>b+AX\geq 0</Math>, we get:
<Math>\newline -100+x\geq 0 \newline</Math>
<Math>200-x \geq 0 \newline</Math>
<Math>-80+y \geq 0 \newline</Math>
<Math>170 -y \geq 0 \newline</Math>
<Math>-200 +x+y \geq 0 \newline</Math>
<Example><![CDATA[
gap> A:= Cdd_PolyhedraByInequalities( [ [ -100, 1, 0 ], [ 200, -1, 0 ], 
> [ -80, 0, 1 ], [ 170, 0, -1 ], [ -200, 1, 1 ] ] );
< Polyhedra given by its H-representation >
gap> Lp:= Cdd_LinearProgram( A, "max", [1, -2, 5 ] );
< Linear program >
gap> S:= Cdd_SolveLinearProgram( Lp );
[ [ 100, 170 ], 651 ]
gap> B:= Cdd_V_Rep( A );
< Polyhedra given by its V-representation >
gap> Display( Lp );
Linear program given by: 
H-representation 
begin 
   5 X 3  rational
                    
   -100     1     0 
    200    -1     0 
    -80     0     1 
    170     0    -1 
   -200     1     1 
end
max  [ 1, -2, 5 ]
gap> Display( B );
V-representation 
begin 
   5 X 3  rational
                  
   1  100  170 
   1  100  100 
   1  120   80 
   1  200   80 
   1  200  170 
end
]]></Example>


So the optimal solution is <Math>(x=100,y=170)</Math> with optimal value <Math>p=1-2(100)+5(170)=651</Math>.

</Section>


</Chapter>

